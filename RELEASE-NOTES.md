# 飯店排班系統 — Release Notes

## v1.1.4

### 本版更新
- 大夜班表整併：移除獨立「大夜班表」頁面，大夜班次輸入整合進排班表總覽：
  - 大夜格位解鎖：可用畫筆或格位微調直接排 D / 休（來源標記維持 night_input，
    自動排班、清空班表均保留大夜手動班次）
  - 非大夜員工不可手動排 D（D 班備援一律由系統指派鏈自動安排）
  - 大夜規則開關（無視所有規則 + H2/H3/H4/H12）移至員工管理的編輯表單
  - 「D 班備援指示」面板移至排班表下方（與支援請求並列），空月份亦可先標記備援日
  - 「啟用 D 班備援邏輯」開關移至休假管理頁（一鍵排班旁）
  - 「清空大夜排班」按鈕移至排班表工具列
  - 格位即時驗證尊重大夜規則開關（無視所有規則 → 不檢查；已關閉的 H4/H12 → 過濾）
  - 修復：大夜手動班表不再導致自動排班失敗（H2/H3/H4/H8/H9/H12 對格位固定的大夜員工
    由求解器豁免，規則開關改為只影響驗證報告顯示）
  - 修復：二館支援的大夜專職（tag + role=night）可排 D1（tag 優先於角色判定）
- 系統版本號升為 1.1.4

## v1.1.3-beta

### 本版更新
- 精準實作經理指定之白天班組合規則（軟性約束 S10 升級）：
  - 週五、週六：4 人白天班（A/B/C）獎勵排 2A2C；3 人白天班獎勵排 1A1B1C（排 ABC）
  - 週日～週四：3 人白天班獎勵排 1A1B1C；白天班達 4 人則懲罰（避免平日排到 4 人）
  - 統計與求解結果同步更新 S10 細項指標（五六2A2C / 五六ABC / 平日ABC / 平日4人）
- 員工管理新增「順序」欄位與上移/下移按鈕，排班表、統計、PDF 依序顯示
- 清空班表後格位顯示為空白（而非紅色「休」）
- 新增員工「標籤」（TAG）機制（如「二館」）：
  - 帶標籤員工視為外部支援，完全手動排班，豁免所有個人排班規則
  - 可排支援班次 A1 / C1 / D1（計入當日 H1 覆蓋），非支援日為空白
  - 排班表員工姓名上方顯示黃色標籤 badge
- 實作跨月連休公平機制（K.1 B 級自動加權）：
  - `data_loader` 自動讀取上月每位員工連休次數
  - 軟性約束 S9 自動針對上月連休未達 2 次（0 或 1 次）者提升獎勵權重（+14），跨月自動輪流平衡連休機會
- 大夜專職略過限制優化：
  - 當大夜人員設定「無視所有規則」（`ignore_all`）時，大夜班表合規驗證直接顯示「已略過所有規則檢驗 ✓」
  - 排班表整月規則驗證報告徹底排除該大夜人員的任何違規與警告
- 手動調整班表操作流程全面升級（快捷畫筆連選 + 批次檢查套用）：
  - 排班表頂部新增「快捷畫筆」工具列（A/B/C/M/休/假別）
  - 支援單擊連選多格快速改班，並以脈衝藍框高亮標示「已暫存修改」
  - 支援一次彙整多處修改進行「檢查並套用」，違規集中預覽並可一鍵「仍要套用（強制）」
  - 支援隨時「放棄」還原所有暫存修改
- 系統版本號升為 1.1.3-beta

## v1.1.2

### 本版更新
- 調整軟性約束 S10（人力加強規則）：
  - 加強日由「週六/週日」改為「週五/週六」
  - 優先序由「B > 雙A > 雙C」改為「雙A = 雙C = M > B」；週五/六不再獎勵 B 班，專注雙 A / 雙 C / M（權重各 +6）
  - 平日（週日～週四）改為優先排 B 班（權重 +3）
- 排班結果與統計頁的 S10 顯示改為「平日 B / 五六雙A / 五六雙C / 五六M」達成天數
- 硬性約束 H3 週末休假限制由「六+日 ≤2」改為「六+日 =2」（自動排班人員恰好 2 天；大夜專職維持 ≤2）
- 班表匯出 PDF 新增圖例（班次色塊 + 指定休假 + 自訂假別）
- 左側欄底部顯示系統版號（後端 /health 提供）
- 自動偵測更新（查 GitHub 最新正式 Release）+ 有新版本時顯示提示橫幅
- 後台關閉改為「只靠關分頁訊號（beacon）」：瀏覽器省電/凍結背景分頁時不再誤關後台
- 啟動時偵測 port 是否已被既有實例占用：占用則直接開瀏覽器連既有實例（單一實例行為）
- 預設埠由 8000 改為 8765（避免與常見開發埠衝突；集中於 config.py）
- 系統版本號升為 1.1.2

## v1.1.0

### 本版更新
- 新增週末人力加強（軟性約束 S10）：週六/週日盡量加人，優先序 B 班 > 雙 A > 雙 C
  - 權重：B 班 +6、雙 A +4、雙 C +2（依優先序遞減）
  - 排班結果與統計頁顯示「週末 B / 雙 A / 雙 C」達成天數
- 整月規則驗證報告補上 S9（連休 2 次優先）與 S10（週末人力加強）
- 排班表「編輯班次」：班別/假別改為兩行單擊按鈕（第一行班別、第二行假別），取代下拉選單
- 系統版本號升為 1.1.0

## v1.0.9

### 本版更新
- 新增班表匯出 PDF（瀏覽器列印，橫式 A4）：
  - 乾淨單純，僅含標題（飯店名 + 年月）、班表格子（員工欄顯示暱稱、日期 + 星期）
  - 週一至週日以加粗框線分組（月首/月尾不滿一週亦各自成框）
  - 週末欄位淺灰底色
- 排班表頁「圖例」移至班表下方、規則驗證上方
- 系統版本號升為 1.0.9-beta

## v1.0.8

### 本版更新
- 員工新增「暱稱」欄位（選填，如英文名 John）；排班表、統計、員工列表等處顯示「暱稱 + 姓名」，休假管理選人改用圓形縮寫 Icon（優先取暱稱首字）
- 休假管理頁介面重構：
  - 人員選擇由下拉選單改為圓形縮寫 Icon 單擊選取
  - 特休 / 事假 / 病假改為獨立按鈕單擊選取（與指定休假一致），自訂假別維持下拉選單
- 一鍵排班整合進休假管理頁：
  - 「開始排班」按鈕置於休假管理頁上方（加大醒目）
  - 「啟用 D 班備援邏輯」開關移至大夜班表頁
  - 「求解時間上限」設定移至統計報表頁（與調班紀錄、匯出同一行）
  - 移除獨立「一鍵排班」頁面與側欄項目
- 排班表頁「規則驗證」區塊預設收合
- 系統版本號升為 1.0.8-beta

## v1.0.7

### 本版更新
- 排班畫面預設顯示「下一個月」（當月通常已排班並發布）
- 當月臨時異動：員工突發請假時，以「最少受影響人數」為目標重排（凍結過去日期、保留現況），預覽 diff 確認後套用並記錄變更
- 系統版本號升為 1.0.7-beta

## v1.0.6

### 本版更新
- D 班備援指派鏈：無可用 CD 備援人員（或該員當日指定休假/請假）時，自動改由管理職補 D；都無法時求解失敗並說明原因，可「略過該備援日並重排」
- 跨月連休公平提示（A 級）：統計頁顯示每人上月連休次數，上月未達 2 次者標記「優先 2 次」
- 系統版本號升為 1.0.6-beta

## v1.0.5

### 本版更新
- 程式碼精簡（無功能變更）：
  - 移除已棄用的啟動入口 `backend/app/main.py`（實際入口為 `run.py`）
  - 刪除重複的排班失敗診斷邏輯，統一由 `diagnose_detailed` 產出
  - 移除未使用的前端依賴 `date-fns`、`class-variance-authority`
  - 移除 `requirements.txt` 中重複的 `ortools` 行
  - 整併重複的輔助函式（`_emp_index`、`_parse_shifts`、`_prev_day1`、班次色盤等）
- 系統版本號升為 1.0.5-beta

## v1.0.4

### 本版更新
- 規格書（Hotel Shift Scheduler.txt）軟硬規則同步為實際實作：
  - H1 每日 B 班最多 1 人（原 2 人）
  - H3 週末休假改為「週六+週日加總 ≤2 天」（原分開各 ≤1）
  - H12 連休改為「每人每月 1~2 次」
  - 刪除 S4（C→D 減少），補上 S8（管理職備援最小化）與 S9（連休 2 次優先）
- 修正排班失敗診斷訊息中的 H3 週末休假規則描述（改為「週六+週日加總最多 2 天」）
- 系統版本號升為 1.0.4

### 下載方式
- Windows：下載 `hotel-scheduler-*-windows-x64.zip`，解壓縮後雙擊 `hotel-scheduler.exe`
- Linux：下載 `hotel-scheduler-*-linux-x64.tar.gz`，解壓縮後執行 `./hotel-scheduler`
- 啟動後瀏覽器會自動開啟 http://localhost:8000

### 系統需求
- Windows 10/11（64 位元）
- Linux（64 位元）
- 不需安裝任何其他軟體

### 功能
- 自動排班引擎（13 硬性 + 8 軟性約束）
- 員工管理（一般 / 大夜專職 / C+D 備援 / 管理職）
- 休假管理（指定休假 / 特休 / 事假 / 病假 / 自訂假別 / 連休計數）
- 跨月設定（銜接合規預覽）
- 大夜班表（手動排班 + 規則開關 + 無視所有規則開關）
- 一鍵排班 + 失敗診斷 + 自動支援偵測
- 手動微調 + 即時合規檢查
- 班表狀態管理 + 版本快照還原 + 調班紀錄
- 請求支援（A/C 班缺人）
- 統計報表 + CSV/JSON 匯出

## v1.0.3

### 本版更新
- 假別擴充與自訂 Tag：內建特休/事假/病假，並可自訂假別（名稱 + 顏色），班表與統計分項顯示
- 班表狀態管理：草稿 → 已發布 → 已鎖定（封存唯讀）；發布/解鎖/鎖定皆有防誤觸
- 版本快照與還原：自動（排班成功/發布）與手動快照、一鍵還原、兩版本 Diff 比對
- 發布後調班紀錄：已發布班表手動微調會記錄（舊班次 → 新班次 + 調班原因）
- 自動支援偵測：排班失敗時自動找出「哪天哪班缺人」並產生支援請求（外部支援視為有人上班，放寬當日覆蓋）
- 規則調整：
  - H1 每日 B 班最多 1 人（原 2 人）
  - H3 週末休假改為「週六+週日加總 ≤2 天」（原分開各 ≤1）
  - H12 連休改為「每人每月 1~2 次」（新增軟性 S9：2 次連休優先、公平分配）
  - 刪除 S4（C→D 減少），保留 S8
- 呈現優化：指定休假獨立酒紅色標示、大夜班顯示班別（小鎖點）、休假管理假別文字標示
- 系統版本號升為 1.0.3

### 下載方式
- Windows：下載 `hotel-scheduler-*-windows-x64.zip`，解壓縮後雙擊 `hotel-scheduler.exe`
- Linux：下載 `hotel-scheduler-*-linux-x64.tar.gz`，解壓縮後執行 `./hotel-scheduler`
- 啟動後瀏覽器會自動開啟 http://localhost:8000

### 系統需求
- Windows 10/11（64 位元）
- Linux（64 位元）
- 不需安裝任何其他軟體

### 功能
- 自動排班引擎（13 硬性 + 8 軟性約束）
- 員工管理（一般 / 大夜專職 / C+D 備援 / 管理職）
- 休假管理（指定休假 / 特休 / 事假 / 病假 / 自訂假別 / 連休計數）
- 跨月設定（銜接合規預覽）
- 大夜班表（手動排班 + 規則開關 + 無視所有規則開關）
- 一鍵排班 + 失敗診斷 + 自動支援偵測
- 手動微調 + 即時合規檢查
- 班表狀態管理 + 版本快照還原 + 調班紀錄
- 請求支援（A/C 班缺人）
- 統計報表 + CSV/JSON 匯出

## v1.0.2

### 本版更新
- 新增「瀏覽器關閉偵測」：關閉瀏覽器分頁後，系統自動結束後台程式，不需再從工作管理員手動結束
  - 正常關分頁：分頁關閉時即時通知後台，約 3 秒後自動關閉
  - 意外當機／強制關閉瀏覽器：心跳超時（約 90 秒）後自動關閉
  - 重新整理分頁不會誤關（延遲關閉期間收到新心跳即取消）
- 新增右上角「關閉系統」按鈕：一鍵結束整個程式（含確認提示）
- 新增跨平台打包：Windows + Linux 雙平台 Release（GitHub Actions 自動打包）
- 系統版本號升為 1.0.2

### 下載方式
- Windows：下載 `hotel-scheduler-*-windows-x64.zip`，解壓縮後雙擊 `hotel-scheduler.exe`
- Linux：下載 `hotel-scheduler-*-linux-x64.tar.gz`，解壓縮後執行 `./hotel-scheduler`
- 啟動後瀏覽器會自動開啟 http://localhost:8000
- 關閉瀏覽器分頁或點右上角「關閉系統」按鈕，程式即自動結束

### 系統需求
- Windows 10/11（64 位元）
- Linux（64 位元）
- 不需安裝任何其他軟體

### 功能
- 自動排班引擎（13 硬性 + 8 軟性約束）
- 員工管理（一般員工 / 大夜專職 / C+D 備援 / 管理職）
- 休假管理（指定休假 / 特休 / 連休計數）
- 跨月設定（銜接合規預覽）
- 大夜班表（手動排班 + 規則開關 + 無視所有規則開關）
- 一鍵排班 + 失敗診斷
- 手動微調 + 即時合規檢查
- 請求支援（A/C 班缺人）
- 統計報表 + CSV/JSON 匯出
- 瀏覽器關閉自動結束 + 一鍵關閉系統按鈕

## v1.0.1-beta

### 本版更新
- 右上角使用者名稱可編輯：點擊名字即可改名，大頭貼字母自動更新
- 排班表頁新增「清空當月排班」按鈕（僅清空自動排班與手動微調，保留大夜手動輸入）
- 大夜班表頁新增「清空大夜排班」按鈕
- 大夜專職新增「無視所有規則」開關（含 H1-H13）：開啟後大夜人員排班不合規定也不影響 A/B/C/M 班自動排班；D 班人力備援與手動固定班表仍正常運作

### 下載方式
1. 下載 hotel-scheduler.zip
2. 解壓縮到任意資料夾
3. 雙擊 hotel-scheduler.exe
4. 瀏覽器會自動開啟 http://localhost:8000

### 系統需求
- Windows 10/11（64 位元）
- 不需安裝任何其他軟體

### 功能
- 自動排班引擎（13 硬性 + 8 軟性約束）
- 員工管理（一般員工 / 大夜專職 / C+D 備援 / 管理職）
- 休假管理（指定休假 / 特休 / 連休計數）
- 跨月設定（銜接合規預覽）
- 大夜班表（手動排班 + 規則開關 + 無視所有規則開關）
- 一鍵排班 + 失敗診斷
- 手動微調 + 即時合規檢查
- 請求支援（A/C 班缺人）
- 統計報表 + CSV/JSON 匯出

## v1.0.0（Phase 1~8 初始開發，2026-08）

依原始規格書分八個 Phase 完成（歷史紀錄；當時規格已由 `SPEC.md` 取代）：

- Phase 1：Backend 骨架 + DB Schema + 基礎 API
- Phase 2：排班引擎核心（硬性約束 H1~H13，CP-SAT）
- Phase 3：軟性約束 + 目標函數
- Phase 4：前端骨架 + 排班表頁面
- Phase 5：員工管理 + 休假管理 UI
- Phase 6：跨月設定 + 大夜班表 UI
- Phase 7：手動微調 + 規則驗證報告
- Phase 8：統計報表 + PyInstaller 打包部署

> 附註：原規格書（Hotel Shift Scheduler.txt）含「開發紀錄附錄」，2026-09 已拆分為
> `SPEC.md`（純現況）、本檔（版本歷史）與 `ROADMAP.md`（未實作方向），原檔移除。
